' <auto-generated />
' このファイルは `pnpm gen` により contract/contract.schema.json から生成されました。
' DO NOT EDIT — 手で編集しないでください。再生成で上書きされます。
Option Strict On
Option Explicit On
Option Infer On

''' <summary>Host → Web の型付きイベント発行ヘルパ。内部で IEventSink.Emit を呼ぶ。</summary>
Public Class BridgeEvents
    Private ReadOnly _sink As IEventSink

    Public Sub New(sink As IEventSink)
        If sink Is Nothing Then Throw New ArgumentNullException(NameOf(sink))
        _sink = sink
    End Sub

    ''' <summary>JSON-RPC notification `event.orderChanged`</summary>
    Public Sub OrderChanged(payload As OrderChangedEvent)
        _sink.Emit("event.orderChanged", payload)
    End Sub

    ''' <summary>JSON-RPC notification `event.heartbeat`</summary>
    Public Sub Heartbeat(payload As HeartbeatEvent)
        _sink.Emit("event.heartbeat", payload)
    End Sub
End Class
